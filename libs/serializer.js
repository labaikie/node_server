'use strict'

module.exports = (data) => {

  let contacts, analysis

  if (data instanceof Object) {

    for(var i in data) {

      switch(i) {

      // reformat data per key if necessary

      }

    }

  }

  return data

}
